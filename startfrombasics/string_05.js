const name= "alok"
const gameCount = 50 

console.log(`hello my name is ${alok} and my game count is ${gameCount}`);

const taskName = new String ('alokfc')
 console.log(taskName.charAt (3));
 console.log(taskName.toUpperCase());
 console.log(taskName.indexOf('o'));
 console.log(taskName.length);


 const newString = taskName.substring(0,4)
 console.log(newString);

 const anotherString = taskName.slice(-6, 1)

 const newStringOne ="  ALOKKU   "

 console.log(newStringOne);
 console.log(newStringOne.trim());


 //A new string representing str stripped of whitespace from both its beginning and end.
 //  Whitespace is defined as white space characters plus line terminators.
//If neither the beginning or end of str has any whitespace, a new string is still returned (essentially a copy of str).